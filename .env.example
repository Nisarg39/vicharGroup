# Vercel Cron-Based Batch Processing Configuration
# Copy to .env.local for local development

# CRITICAL: Cron Authentication (Required for Production)
# Generate a secure random string for production deployment
# Example: openssl rand -base64 32
VERCEL_CRON_SECRET=your-secure-secret-key-here

# Batch Processing Configuration (Optional - has defaults)
EXAM_BATCH_SIZE=20
CRON_MAX_PROCESSING_TIME=750000

# Auto-detected by Vercel (Do not set manually)
# VERCEL=1
# VERCEL_ENV=production

# Database Configuration (Existing)
MONGODB_URI=your-mongodb-connection-string

# Other existing environment variables...
# Add your existing .env variables below this line